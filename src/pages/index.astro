---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="Dark Beer Mile — run a mile, drink dark beer every 400 meters."
		/>
		<title>Dark Beer Mile</title>
	</head>
	<body>
		<main class="hero">
			<h1>Dark Beer Mile</h1>
			<p class="tagline">Run a mile. Drink dark beer. Repeat.</p>
			<p class="coming-soon">Coming soon — site launching shortly.</p>
		</main>
	</body>
</html>

<style>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html,
	body {
		height: 100%;
		background: #0a0a0a;
		color: #111;
	}

	body {
		display: flex;
		align-items: center;
		justify-content: center;
		font-family: system-ui, -apple-system, sans-serif;
	}

	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 1.25rem;
		padding: 2rem;
		text-align: center;
	}

	h1 {
		color: #111;
		font-size: clamp(1.75rem, 5vw, 3rem);
		font-weight: 300;
		letter-spacing: 0.35em;
		text-transform: uppercase;
		text-shadow: 0 0 1px rgba(255, 255, 255, 0.06);
	}

	.tagline {
		color: #1a1a1a;
		font-size: clamp(0.95rem, 2.5vw, 1.15rem);
		font-weight: 400;
		letter-spacing: 0.08em;
		text-shadow: 0 0 1px rgba(255, 255, 255, 0.04);
	}

	.coming-soon {
		margin-top: 0.5rem;
		color: #222;
		font-size: 0.85rem;
		font-weight: 300;
		letter-spacing: 0.2em;
		text-transform: uppercase;
	}
</style>
